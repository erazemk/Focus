# Focus

A tiny macOS menu bar app to track how long you're focused for.

If you downloaded a release, macOS may quarantine it.
To remove the quarantine attribute run `xattr -dr com.apple.quarantine Focus.app`.
